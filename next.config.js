/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactStrictMode: true,
  // Se você precisar de alguma configuração específica para imagens, pode adicionar aqui:
  // images: {
  //   domains: ['example.com'], // Exemplo: se usar imagens de domínios externos
  // },
};

module.exports = nextConfig; // <-- MUDANÇA AQUI: exporta usando module.exports